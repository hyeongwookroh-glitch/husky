---
description: Run the Husky installation flow (persona interview, MCP setup, plugin guidance)
---

Read `INSTALL.md` in the project root and follow it step by step. Do not skip steps, do not combine stages, do not call MCP tools (the agent is not yet configured).

If `.claude/setup-complete` already exists, report "이미 설치 완료됨" and explain how to re-install (delete `.claude/setup-complete`, `CLAUDE.md`, `.claude/settings.json`, `.env`).
